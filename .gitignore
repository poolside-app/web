# OS / editor noise
.DS_Store
Thumbs.db
*.swp

# Vendored CLI binaries (~30 MB) — devs install their own
tools/

# Local env
.env
.env.local
.vercel

# Supabase CLI temp
supabase/.temp/

# Node/build artifacts (when we add a build step)
node_modules/
dist/
.next/
.cache/

# Logs / scratch
*.log
recovery.txt
